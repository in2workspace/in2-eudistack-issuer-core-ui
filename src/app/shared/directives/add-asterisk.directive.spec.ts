import { AddAsteriskDirective } from './add-asterisk.directive';

describe('AddAsteriskDirective', () => {
  it('should create an instance', () => {
    const directive = new AddAsteriskDirective();
    expect(directive).toBeTruthy();
  });
});
